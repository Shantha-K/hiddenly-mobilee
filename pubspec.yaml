name: inochat
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.7.2
  http: ^1.5.0
  shared_preferences: ^2.2.2
  local_auth: ^2.3.0
  permission_handler: ^11.3.1
  flutter_contacts: ^1.1.9+2
  url_launcher: ^6.2.6
  socket_io_client: ^3.1.2
  google_sign_in: ^6.2.1
  googleapis: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1  

flutter_icons:  # ✅ this is where the configuration goes
  android: true
  ios: false
  image_path: "assets/images/app_icon.png"

flutter:
  uses-material-design: true
